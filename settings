# separator in database
SEPARATOR = =

# max size of file, in bytes
MAX_FILE_SIZE = 20

# path to data directory
PATH_DATA_DIRECTORY = data/

# module for calculating hash of strings
MODULE = 576460752303423619 # big prime module ~= 5e17

# coefficient for calculating hash of strings
COEFFICIENT = 97 # should be more than alphabet size

# GCD(MODULE, COEFFICIENT) should be equals to 1